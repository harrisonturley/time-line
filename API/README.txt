API Docs: http://localhost:4000/apiDocs
You will need the Yelp API private key to use the search endpoint. Ask Mark for it.
Run Server: npm run server


to build and install dependencies: run npm install in the API folder
all the api routes other than the docs are at http://localhost:4000/api/..route

Set the environment variable for GOOGLE_APPLICATION_CREDENTIALS, ask Victoria for value. 

need to keep the yelp api key stored in API/api_keys/yelpApiKeys.js 
currently is git ignored so you need to make your own. ask victoria for the yelpApiKeys.js file

mongodb
database name: time-line
collection: users